# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true
    active:
        label: Active
        type: switch
    name:
        label: Name
        type: text
    parent:
        lable: Category
        relation: parent
        select: name
    subcategories_count:
        label: Subcategories
        relation: subcategories
        useRelationCount: true
        type: number
    productCount:
        label: Products
        type: number
    nestedCount:
        label: Total Products
        type: number
