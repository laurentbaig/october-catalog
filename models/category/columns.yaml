# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true
    active:
        label: Active
        type: switch
    name:
        label: Name
        type: text
    parent:
        lable: Category
        relation: parent
        select: name
